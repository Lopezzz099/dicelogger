# Base de datos MySQL
DB_USERNAME=
DB_PASSWORD=
DB_HOST=
DB_PORT=3306
DB_NAME=

# Puerto del servidor (Railway/Render/Fly lo inyectan solos; en local queda en 8080)
PORT=8080

# Firebase Auth — además de estas vars, hace falta el archivo
# serviceAccountKey.json en la raíz del backend (no se versiona, pedirlo
# de nuevo desde la consola de Firebase del proyecto logoflegends-19383)

# Envío de emails (Gmail SMTP con contraseña de aplicación, NO la de la cuenta)
EMAIL_SENDER=diceloggerdnd@gmail.com
EMAIL_APP_PASSWORD=

# AWS S3 (subida de imágenes desde el backend, si aplica)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=sa-east-1
